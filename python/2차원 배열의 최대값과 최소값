nums = [[1, 2, 3], [1, 4, 5], [4, 5, 6], [0, 1, 2], [0, 1, 1]]

print(min(nums))
[0, 1, 1] //첫번째 원소의 값이 가장 작은 원소가 출력됨



max(map(max, vertices))// 전체 범위에서 제일 큰 값
min(map(min, vertices))// 전체 범위에서 제일 작은 값

